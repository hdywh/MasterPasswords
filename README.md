# MasterPasswords
This program for storing passwords for sites and services contains an intuitive interface. To log in, a master password is used, which can be set once and is encrypted in a separate file. Passwords are stored locally in json format.
